<tool id="rpCofactors" name="RP Cofactors" version="1.0">
  <description>Add the cofactors to the mono-component reactions generated by RetroPath2.0</description>
  <requirements>
		<requirement type="package">requests</requirement>
  </requirements>
  <stdio>
    <regex match="WARNING:" level="warning" />
	  <regex match="ERROR:" level="error" />
  </stdio>
  <command detect_errors="exit_code"><![CDATA[
    '$__tool_directory__/tool_rpCofactors.py' -input '$input' -output '$output' -format '$adv.format' -pathway_id '$adv.pathway_id' -compartment_id '$adv.compartment_id' -server_url '$adv.server_url'
  ]]></command>
  <inputs>
    <param name="inputTar" type="data" format="tar" label="rpSBML" />
    <section name="adv" title="Advanced Options" expanded="false">
      <param name="pathway_id" type="text" value="rp_pathway" label="Name of the heterologous pathway" />
      <param name="compartment_id" type="text" value="MNXC3" label="SBML compartment ID" />
      <param name="server_url" type="text" value="http://0.0.0.0:8888/REST" label="REST IP address" />
      <conditional name="format_input">
        <param name="format" type="select" label="Input/output format: SBML or TAR">
          <option value="sbml" selected="True">SBML input/output</option>
          <option value="tar">TAR input/output</option>
        </param>
      </conditional>
    </section>
  </inputs>
  <outputs>
    <data name="output" format="tar" label="rpCofactors">
      <change_format>
        <when input="format" value="sbml" format="sbml"/>
        <when input="format" value="tar" format="tar"/>
      </change_format>
    </data>
  </outputs>
  <help>
		Takes for input a tar of rpSBML files and completes the monocomponent reactions with full ones
  </help>
</tool>
